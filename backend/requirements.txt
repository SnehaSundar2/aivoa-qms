# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Database ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3        # PostgreSQL driver
PyMySQL==1.1.1                # alternative if you point DATABASE_URL at MySQL

# --- AI agent ---
langgraph>=0.2.60,<0.3
langchain-groq>=0.2.3,<0.4
# langchain-core is pulled in by the two above; left unpinned so pip can
# resolve a version compatible with both.

# --- Document intake ---
pypdf==5.1.0
Pillow==11.0.0                # image validation and downscaling for the vision path

# --- Optional local OCR, used only if a Groq vision model is not configured.
# Needs the Tesseract binary installed separately.
# pytesseract==0.3.13
